@code {
    [Parameter]
    public EventCallback AsyncOpertaion { get; set; }

    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
        {
            AsyncOpertaion.InvokeAsync();
        }
    }
}
